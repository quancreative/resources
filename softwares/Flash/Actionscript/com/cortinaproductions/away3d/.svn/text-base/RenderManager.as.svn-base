package com.cortinaproductions.away3d{	import away3d.containers.View3D;	import flash.events.Event;		public class RenderManager	{				private var view			:View3D;				private var rendering		:Boolean = false;		//private var dict			:Dictionary;		private var mclip			:Object;				function RenderManager(_mc:Object, _view:View3D)		{			//dict = new Dictionary();			mclip = _mc;						view = _view;						_mc.addEventListener(Event.ENTER_FRAME, renderInterval,false,0,true);				}						public function render():void 		{			if (!rendering){				rendering = true;						}		}						private function renderInterval(e:Event):void 		{			if (rendering) {								rendering = false;							render3d();			}					}				// draw 3d graphics to screen		private function render3d():void 		{													view.render();		}						public function destroy():void 		{			mclip.removeEventListener(Event.ENTER_FRAME, renderInterval);					mclip = null;			//dict["source"].removeEventListener(Event.ENTER_FRAME, renderInterval);					//dict = null;						view = null;		}							}		}